version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install pipenv
      - pipenv install
      - pip3 install -r requirements.txt
run:
  runtime-version: 3.8
  command: python app.py
  network:
    port: 8050
